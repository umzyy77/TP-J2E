apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init
  namespace: masterannonce
data:
  01-init.sql: |
    -- Schema JPA/Hibernate pour MasterAnnonce
    CREATE EXTENSION IF NOT EXISTS pgcrypto;

    -- Table Roles (RBAC dynamique)
    CREATE TABLE IF NOT EXISTS roles (
        id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        name VARCHAR(50) NOT NULL UNIQUE
    );

    -- Table Users
    CREATE TABLE IF NOT EXISTS users (
        id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
        username VARCHAR(50) NOT NULL UNIQUE,
        email VARCHAR(100) NOT NULL UNIQUE,
        password VARCHAR(255) NOT NULL,
        role_id BIGINT NOT NULL REFERENCES roles(id),
        created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
    );

    -- Autorites rattachees aux roles
    CREATE TABLE IF NOT EXISTS role_authorities (
        role_id BIGINT NOT NULL REFERENCES roles(id) ON DELETE CASCADE,
        authority VARCHAR(64) NOT NULL,
        PRIMARY KEY (role_id, authority)
    );

    -- Table Category
    CREATE TABLE IF NOT EXISTS category (
        id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        label VARCHAR(100) NOT NULL UNIQUE
    );

    -- Table Annonce
    CREATE TABLE IF NOT EXISTS annonce (
        id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        title VARCHAR(64) NOT NULL,
        description VARCHAR(256) NOT NULL,
        adress VARCHAR(64) NOT NULL,
        mail VARCHAR(64) NOT NULL,
        date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
        status VARCHAR(20) NOT NULL DEFAULT 'DRAFT',
        version BIGINT DEFAULT 0,
        author_id UUID NOT NULL REFERENCES users(id),
        category_id BIGINT NOT NULL REFERENCES category(id)
    );

    -- Index pour les recherches
    CREATE INDEX IF NOT EXISTS idx_annonce_status ON annonce(status);
    CREATE INDEX IF NOT EXISTS idx_annonce_date ON annonce(date DESC);
    CREATE INDEX IF NOT EXISTS idx_annonce_author ON annonce(author_id);
    CREATE INDEX IF NOT EXISTS idx_annonce_category ON annonce(category_id);

  02-seed.sql: |
    -- Donnees de seed pour MasterAnnonce (K8s)
    -- Mot de passe: password123, BCrypt 12 rounds
    -- Hash pre-calcule pour eviter les commandes psql specifiques

    -- Roles
    INSERT INTO roles (id, name) VALUES
        (1, 'ROLE_ADMIN'),
        (2, 'ROLE_USER')
    ON CONFLICT DO NOTHING;

    SELECT setval(pg_get_serial_sequence('roles', 'id'), (SELECT MAX(id) FROM roles), true);

    -- Autorites par role
    INSERT INTO role_authorities (role_id, authority) VALUES
        (1, 'ANNONCE_ARCHIVE')
    ON CONFLICT DO NOTHING;

    -- Utilisateurs (password123 hache BCrypt 12 rounds)
    INSERT INTO users (id, username, email, password, role_id, created_at) VALUES
        ('11111111-1111-1111-1111-111111111111', 'admin', 'admin@masterannonce.fr', '$2b$12$IU1G75QfUd8UhGwcXNlLI.miVtJFcT5ZCmVE8SlxNrEOvI.MM474m', 1, NOW()),
        ('22222222-2222-2222-2222-222222222222', 'jean', 'jean@example.com', '$2b$12$IU1G75QfUd8UhGwcXNlLI.miVtJFcT5ZCmVE8SlxNrEOvI.MM474m', 2, NOW()),
        ('33333333-3333-3333-3333-333333333333', 'marie', 'marie@example.com', '$2b$12$IU1G75QfUd8UhGwcXNlLI.miVtJFcT5ZCmVE8SlxNrEOvI.MM474m', 2, NOW()),
        ('44444444-4444-4444-4444-444444444444', 'lucas', 'lucas@example.com', '$2b$12$IU1G75QfUd8UhGwcXNlLI.miVtJFcT5ZCmVE8SlxNrEOvI.MM474m', 2, NOW()),
        ('55555555-5555-5555-5555-555555555555', 'sophie', 'sophie@example.com', '$2b$12$IU1G75QfUd8UhGwcXNlLI.miVtJFcT5ZCmVE8SlxNrEOvI.MM474m', 2, NOW()),
        ('66666666-6666-6666-6666-666666666666', 'karim', 'karim@example.com', '$2b$12$IU1G75QfUd8UhGwcXNlLI.miVtJFcT5ZCmVE8SlxNrEOvI.MM474m', 2, NOW()),
        ('77777777-7777-7777-7777-777777777777', 'claire', 'claire@example.com', '$2b$12$IU1G75QfUd8UhGwcXNlLI.miVtJFcT5ZCmVE8SlxNrEOvI.MM474m', 2, NOW()),
        ('88888888-8888-8888-8888-888888888888', 'mehdi', 'mehdi@example.com', '$2b$12$IU1G75QfUd8UhGwcXNlLI.miVtJFcT5ZCmVE8SlxNrEOvI.MM474m', 2, NOW())
    ON CONFLICT DO NOTHING;

    -- Categories
    INSERT INTO category (id, label) VALUES
        (1, 'Immobilier'),
        (2, 'Emploi'),
        (3, 'Services'),
        (4, 'Vehicules'),
        (5, 'Formation')
    ON CONFLICT DO NOTHING;

    SELECT setval(pg_get_serial_sequence('category', 'id'), (SELECT MAX(id) FROM category), true);

    -- Annonces de test
    INSERT INTO annonce (title, description, adress, mail, date, status, author_id, category_id) VALUES
        ('Location studio Paris 15e', 'Studio lumineux proche transports, 25m2, cuisine equipee.', '12 rue des Lilas, Paris 15', 'contact@studio.fr', NOW() - INTERVAL '1 day', 'PUBLISHED', '11111111-1111-1111-1111-111111111111', 1),
        ('Colocation 3 pieces Saint-Denis', 'Chambre disponible dans appartement 3 pieces, 14m2.', '7 rue Victor Hugo, Saint-Denis', 'coloc@annonce.fr', NOW() - INTERVAL '2 days', 'DRAFT', '22222222-2222-2222-2222-222222222222', 1),
        ('Appartement T3 Montreuil', 'T3 refait a neuf, balcon, proche metro Robespierre.', '25 boulevard Rouget de Lisle, Montreuil', 'appart.montreuil@mail.fr', NOW() - INTERVAL '3 days', 'PUBLISHED', '33333333-3333-3333-3333-333333333333', 1),
        ('Stage dev web React/Node', 'Stage 3 mois en developpement web React/Node.', 'Campus Galilee, Villetaneuse', 'stage@entreprise.fr', NOW() - INTERVAL '1 day', 'PUBLISHED', '11111111-1111-1111-1111-111111111111', 2),
        ('CDI Developpeur Java Senior', 'Recherche dev Java 5 ans exp, Spring Boot, microservices.', '100 avenue de France, Paris 13', 'recrutement@techcorp.fr', NOW() - INTERVAL '2 days', 'PUBLISHED', '44444444-4444-4444-4444-444444444444', 2),
        ('Plombier urgence 93', 'Plombier disponible 7j/7, depannage rapide.', 'Secteur Seine-Saint-Denis', 'plombier93@service.fr', NOW() - INTERVAL '2 days', 'PUBLISHED', '66666666-6666-6666-6666-666666666666', 3),
        ('Vente Renault Clio 2018', 'Clio 2018, 50000km, tres bon etat, CT OK.', '45 avenue Jean Jaures, Bobigny', 'clio@annonces.com', NOW() - INTERVAL '1 day', 'PUBLISHED', '33333333-3333-3333-3333-333333333333', 4),
        ('Cours de Java Jakarta EE', 'Cours particuliers Java/Jakarta pour debutants.', 'Universite Paris 13, Villetaneuse', 'java.tutor@mail.com', NOW() - INTERVAL '1 day', 'PUBLISHED', '22222222-2222-2222-2222-222222222222', 5)
    ON CONFLICT DO NOTHING;
